// Twitter / X image — same generator as OG, exported separately so Next
// emits both `og:image` and `twitter:image` meta tags.
export { default } from "./opengraph-image";
export {
  runtime,
  alt,
  size,
  contentType,
} from "./opengraph-image";
