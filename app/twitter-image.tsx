// Twitter / X card image — same generator as the OG image, with the
// route segment config inlined (Next.js won't accept re-exported
// runtime/size/contentType fields, they must be statically parseable
// per-file).
export { default } from "./opengraph-image";

export const runtime = "edge";
export const alt =
  "AgentRadar — see your store through AI shoppers' eyes. Five real LLMs from five distinct providers run against any Shopify catalog and show the literal transcripts.";
export const size = { width: 1200, height: 630 };
export const contentType = "image/png";
